name: close rejected
on:
  schedule:
    - cron: '30 1 * * *'

jobs:
  stale:
    runs-on: ubuntu-latest
    permissions:
      issues: write
      pull-requests: write
    steps:
      - uses: actions/stale@v4
        with:
          repo-token: ${{ secrets.PAT }}
          only-labels: 'rejected'
          stale-issue-label: 'stale'
          days-before-stale: 4
          days-before-close: 1
